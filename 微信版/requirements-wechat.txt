# 微信机器人依赖（在 Windows 的 Python 3.12 环境安装）
# 安装：venv\Scripts\python.exe -m pip install -r requirements-wechat.txt

# 微信自动化（微信 4.x 用 wechatauto-replica，注意导入名是 wechatauto）
wechatauto-replica

# LangChain + DeepSeek（DeepSeek V4.1 Flash 原生支持识图，无需第三方视觉模型）
langchain>=1.3
langchain-core>=1.6
langchain-deepseek>=1.1
langchain-text-splitters>=1.0

# .env 读取
python-dotenv>=1.0

# 语音合成（轻量，无需 GPU）
edge-tts>=7.0
